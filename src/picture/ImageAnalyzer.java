package picture;

import java.util.ArrayDeque;
import java.util.Deque;

public class ImageAnalyzer {

  //bfs odata
  public Zone createZone(Color startPixel, Zone zone) {
    //zone.pixelList.add(startPixel);
    Deque<Color> queue = new ArrayDeque<>();
    queue.add(startPixel);

    while (queue.size() > 0) {

    }


  }

  //bfs pe toata imaginea
}
